# yaml-language-server: $schema=https://agentgateway.dev/schema/config
config:
  readinessAddr: 0.0.0.0:15021

llm:
  port: 4000
  models:
  - name: "*"
    provider: anthropic
    params:
      apiKey: mock-provider-key
      baseUrl: http://anthropic-mock:8000/v1
