{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "DaemonConfig",
  "description": "Local startup configuration for an AgentDesktop daemon.",
  "type": "object",
  "properties": {
    "controller": {
      "description": "Controller connection settings. Omit this field to run without fleet management.",
      "anyOf": [
        {
          "$ref": "#/$defs/ControllerConfig"
        },
        {
          "type": "null"
        }
      ]
    },
    "inferenceGateways": {
      "description": "Named inference gateways used as the local desired-state baseline.\n\nNames may contain letters, numbers, `.`, `-`, and `_`.",
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/InferenceGatewayConfig"
      }
    },
    "programs": {
      "description": "Per-program settings used as the local desired-state baseline.",
      "$ref": "#/$defs/ProgramsConfig"
    }
  },
  "additionalProperties": false,
  "$defs": {
    "ClaudeCodeConfig": {
      "description": "Settings reconciled into Claude Code's managed configuration.\n\nKeys other than `inferenceGateway` are written directly to AgentDesktop's\nmanaged-settings drop-in. When generated gateway settings overlap with\npass-through values, AgentDesktop's generated values take precedence.",
      "type": "object",
      "properties": {
        "inferenceGateway": {
          "description": "Name of an entry in `inferenceGateways` that Claude Code should use.\n\nOmit this field to manage Claude Code settings without configuring an inference gateway.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": true
    },
    "CodexConfig": {
      "description": "Settings reconciled into Codex's organization-managed configuration.\n\nValues under `managedConfig` are written to Codex's `managed_config.toml`.\nWhen generated inference-gateway settings overlap with those values,\nAgentDesktop's generated values take precedence.",
      "type": "object",
      "properties": {
        "inferenceGateway": {
          "description": "Name of an entry in `inferenceGateways` that Codex should use.\n\nOmit this field to manage general Codex settings without configuring an inference gateway.",
          "type": [
            "string",
            "null"
          ]
        },
        "managedConfig": {
          "description": "Arbitrary values written to Codex's organization-managed TOML configuration.\n\nUse Codex's native snake_case configuration keys. TOML has no null value,\nso null values cannot be reconciled.",
          "type": "object",
          "additionalProperties": true
        }
      },
      "additionalProperties": false
    },
    "ControllerConfig": {
      "description": "Connection settings for the fleet controller.",
      "type": "object",
      "properties": {
        "address": {
          "description": "HTTP or HTTPS address of the controller's fleet API.",
          "type": "string"
        },
        "caCertificatePath": {
          "description": "Path to a PEM-encoded CA certificate used to verify the controller.\n\nOmit this field to use the operating system's trusted certificate roots.",
          "type": [
            "string",
            "null"
          ]
        },
        "heartbeatInterval": {
          "description": "Interval between device heartbeats. Defaults to `30s`.",
          "type": "string",
          "default": "30s"
        }
      },
      "additionalProperties": false,
      "required": [
        "address"
      ]
    },
    "InferenceGatewayAuthentication": {
      "description": "Authentication mechanisms supported by an inference gateway.",
      "oneOf": [
        {
          "description": "Request a short-lived JWT from the controller using the device identity.",
          "type": "object",
          "properties": {
            "audience": {
              "description": "Audience placed in the issued JWT. This must match the gateway's expected audience.",
              "type": "string"
            },
            "type": {
              "type": "string",
              "const": "controllerJwt"
            }
          },
          "additionalProperties": false,
          "required": [
            "type",
            "audience"
          ]
        }
      ]
    },
    "InferenceGatewayConfig": {
      "description": "Connection and authentication settings for an inference gateway.",
      "type": "object",
      "properties": {
        "authentication": {
          "description": "Authentication mechanism used when connecting to this gateway.",
          "anyOf": [
            {
              "$ref": "#/$defs/InferenceGatewayAuthentication"
            },
            {
              "type": "null"
            }
          ]
        },
        "url": {
          "description": "Base HTTP or HTTPS URL of the inference gateway.\n\nThe URL must include a host and cannot include credentials, a query, or a fragment.",
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "url"
      ]
    },
    "OpenCodeConfig": {
      "description": "Settings reconciled into OpenCode's system-managed configuration.\n\nValues under `managedConfig` are written to OpenCode's managed JSONC file.\nWhen generated inference-gateway settings overlap with those values,\nAgentDesktop's generated values take precedence.",
      "type": "object",
      "properties": {
        "inferenceGateway": {
          "description": "Name of an entry in `inferenceGateways` that OpenCode should use.\n\nOmit this field to manage general OpenCode settings without configuring an inference gateway.",
          "type": [
            "string",
            "null"
          ]
        },
        "managedConfig": {
          "description": "Arbitrary values written to OpenCode's system-managed configuration.",
          "type": "object",
          "additionalProperties": true
        },
        "model": {
          "description": "Model ID selected from `models` when using the inference gateway.\n\nThis is required when `inferenceGateway` is set.",
          "type": [
            "string",
            "null"
          ]
        },
        "models": {
          "description": "Models exposed by the managed inference-gateway provider, keyed by model ID.\n\nEach value is an arbitrary OpenCode model configuration object. At least\none model is required when `inferenceGateway` is set.",
          "type": "object",
          "additionalProperties": true
        }
      },
      "additionalProperties": false
    },
    "ProgramsConfig": {
      "description": "Settings for developer tools managed by AgentDesktop.",
      "type": "object",
      "properties": {
        "claudeCode": {
          "description": "Claude Code managed-settings configuration. Arbitrary keys are passed through directly.",
          "anyOf": [
            {
              "$ref": "#/$defs/ClaudeCodeConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "codex": {
          "description": "Codex managed configuration.",
          "anyOf": [
            {
              "$ref": "#/$defs/CodexConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "openCode": {
          "description": "OpenCode managed configuration.",
          "anyOf": [
            {
              "$ref": "#/$defs/OpenCodeConfig"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false
    }
  }
}
