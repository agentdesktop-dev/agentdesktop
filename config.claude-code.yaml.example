programs:
  claudeCode:
    managedSettings:
      companyAnnouncements:
        - This Claude Code installation is managed by Agentplane.
      permissions:
        deny:
          - Read(./.env)
          - Read(./.env.*)
