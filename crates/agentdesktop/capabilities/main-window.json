{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "main-window",
  "description": "Allows the local settings window to call Agent Desktop commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
